import React from 'react';
import PropTypes from 'prop-types';
import '../style.scss';

const Root = props => {
    return (
        <div>
            <h3 className="title">Hello, React</h3>
        </div>
    );
};

Root.propTypes = {

};

export default Root;